# Hostname used when accessing `next dev` through a Cloudflare tunnel (Next.js allowedDevOrigins).
# Copy to .env.local and set to the hostname in your tunnel ingress config.
# NEXT_DEV_TUNNEL_HOST=dev.example.com

# Set to 1 to disable Serwist service worker during local dev.
# DISABLE_SW=1
